<!DOCTYPE html>
<html>
<head>
  <title>TurntableExporter</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="<%= root_url %>">Turntable.fm Exporter</a>
        </div>
        <div class="collapse navbar-collapse">
            <% if user_signed_in? %>
                <p class="navbar-text pull-right">Signed in as <strong><%= @current_user.nickname %></strong> | <a class="navbar-link" href="<%= signout_url %>">Sign Out</a></p>
            <% end %>
        </div>
    </div>
</div>
<div class="container" role="content">
    <% flash.each do |name, msg| %>
        <% if name == :alert %>
            <div class="alert alert-danger"><%= msg %></div>
        <% elsif name == :notice %>
            <div class="alert alert-success"><%= msg %></div>
        <% else %>
            <div class="alert alert-info"><%= msg %></div>
        <% end %>
    <% end %>
<%= yield %>
</div>
<footer class="container">
    <div><small><a href="http://plug.dj">Plug.dj</a> is ©2013 Plug DJ, LLC. | <a href="http://turntable.fm">turntable.fm</a> is © 2013 turntable.fm | <a href="http://soundcloud.com">Soundcloud</a> is © 2013 Soundcloud</small></div>
</footer>
</body>
</html>
