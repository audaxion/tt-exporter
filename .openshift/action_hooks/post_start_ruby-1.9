#!/bin/bash

set -e

pushd ${OPENSHIFT_REPO_DIR} > /dev/null

echo "Starting sidekiq..."
sleep 15
bundle exec "sidekiq -d -r ./ -c 5 -e production -P '${OPENSHIFT_DATA_DIR}sidekiq.pid' -L '${OPENSHIFT_DATA_DIR}sidekiq.log'"

popd